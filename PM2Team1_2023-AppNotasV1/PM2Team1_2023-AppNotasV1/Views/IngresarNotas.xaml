<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:xct="http://xamarin.com/schemas/2020/toolkit" 
             xmlns:viewmodels="clr-namespace:PM2Team1_2023_AppNotasV1.ViewModels" x:DataType="viewmodels:NotasViewModel"
             x:Class="PM2Team1_2023_AppNotasV1.Views.IngresarNotas">
    <ContentPage.Content>
        <StackLayout Padding="10" HorizontalOptions="FillAndExpand">
            <Label Text="Agregar Nueva Nota" FontSize="Large" VerticalOptions="Center" HorizontalTextAlignment="Center"></Label>
            <Entry x:Name="txtTitu" Placeholder="Título"  Text="{Binding Titulo, Mode=TwoWay}"/>
            <Editor x:Name="txtDetal" Placeholder="Nota de Texto"  HeightRequest="50" Text="{Binding Detalles, Mode=TwoWay}" />

            <FlexLayout Direction="Row" JustifyContent="SpaceBetween" Margin="0,0,0,0">
                <Label  Text="Longitud: " />
                <Label Text="Latitud: " />
            </FlexLayout>

            <FlexLayout Direction="Row" JustifyContent="SpaceBetween" Margin="0,0,0,0">
              
                <Entry x:Name="txtLongi" Text="{Binding Longitud, Mode=TwoWay}"/>
               
                <Entry x:Name="txtLatit" Text="{Binding Latitude, Mode=TwoWay}"  />
            </FlexLayout>



            <FlexLayout Direction="Row" JustifyContent="SpaceBetween" Margin="0,0,0,0">
                <Label Text="Seleccione la hora" TextColor="Black" FontSize="16" HorizontalOptions="Center" />
                <Label Text="Seleccione la hora" TextColor="Black" FontSize="16" HorizontalOptions="Center" />
            </FlexLayout>

            <!-- FlexLayout para alinear horizontalmente el DatePicker y el TimePicker -->
            <FlexLayout Direction="Row" JustifyContent="SpaceBetween" Margin="0,0,0,0">
                <!-- Calendario (DatePicker) -->
                <DatePicker x:Name="dtFecha" TextColor="Black" FlexLayout.AlignSelf="Start" Margin="0,0,0,0" />
                <!-- Reloj (TimePicker) -->
                <TimePicker x:Name="dtHora" TextColor="Black" FlexLayout.AlignSelf="End" Margin="0,0,0,0" />
            </FlexLayout>
            
            <Label x:Name="lbAudio" />
            <Button  Text="Agregar Audio" Clicked="AgregarAudio_Clicked" />

         
            <Button Text="Agregar Fotografía" Clicked="AgregarFotografia_Clicked" />
            <Image x:Name="imageField" WidthRequest="120" BackgroundColor="Gray"/>
            <Button Text="Guardar" Clicked="BtnGuardar_Clicked" Command="{Binding insertCommand}"/>
            <Image Source="{Binding ImageData}" WidthRequest="50" HeightRequest="50" />
            <Label Text="{Binding ImageName}" VerticalOptions="Center" />
           

        </StackLayout>
    </ContentPage.Content>
</ContentPage>